Run only in Windows
If you have python 3 installed in your PC then run "Survey.exe"
else run "Setup.exe" and install python 3(You need internet connection for this)

You need to start the app(Survey.exe) again to do the other surveys after attempting one.